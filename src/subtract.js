/**
 * Subtracts two numbers
 * @param {*} num1 - The first number
 * @param {*} num2 - The second number
 * @returns The difference between the first and second number
 */

function subtract(num1, num2) {
  return num1 - num2;
}
